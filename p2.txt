Sure! Here are 30 tricky and complex programming questions that you can use to prepare for a Zoho MNC interview, covering various concepts including pointers, arrays, strings, memory management, control structures, and more:

### 1. **Pointer Arithmetic**
```c
#include <stdio.h>

int main() {
    int arr[] = {10, 20, 30, 40, 50};
    int *ptr = arr + 2;
    printf("%d\n", *(ptr - 1) + *(ptr + 1));
    return 0;
}
```
**Output:**
```
70
```
**Explanation:** Pointer arithmetic is used to access elements in the array.

### 2. **Array and Pointer Confusion**
```c
#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    printf("%d\n", *arr + 2);
    return 0;
}
```
**Output:**
```
12
```
**Explanation:** `*arr` gives the first element (10), and adding 2 results in 12.

### 3. **Memory Overlap with Arrays**
```c
#include <stdio.h>

int main() {
    int a[5] = {1, 2, 3, 4, 5};
    int *ptr = a;
    printf("%d\n", ptr[3]);
    return 0;
}
```
**Output:**
```
4
```
**Explanation:** Pointer `ptr` points to the array `a`. `ptr[3]` accesses the 4th element (index 3).

### 4. **Understanding Static Variables**
```c
#include <stdio.h>

void fun() {
    static int count = 0;
    count++;
    printf("%d\n", count);
}

int main() {
    fun();
    fun();
    fun();
    return 0;
}
```
**Output:**
```
1
2
3
```
**Explanation:** The `static` variable retains its value between function calls.

### 5. **String Manipulation with Pointers**
```c
#include <stdio.h>

int main() {
    char str[] = "Programming";
    char *ptr = str;
    ptr++;
    printf("%c\n", *ptr);
    return 0;
}
```
**Output:**
```
r
```
**Explanation:** The pointer moves to the second character (`r`), which is printed.

### 6. **Structure Padding**
```c
#include <stdio.h>

struct example {
    char a;
    int b;
    char c;
};

int main() {
    printf("%lu\n", sizeof(struct example));
    return 0;
}
```
**Output:**
```
12
```
**Explanation:** Padding is added to ensure alignment of variables in the structure.

### 7. **Pointer to Pointer**
```c
#include <stdio.h>

int main() {
    int val = 10;
    int *ptr = &val;
    int **pptr = &ptr;
    printf("%d\n", **pptr);
    return 0;
}
```
**Output:**
```
10
```
**Explanation:** `**pptr` dereferences the pointer twice to get the value.

### 8. **Function Pointer**
```c
#include <stdio.h>

void greet() {
    printf("Hello, World!\n");
}

int main() {
    void (*func_ptr)() = greet;
    func_ptr();
    return 0;
}
```
**Output:**
```
Hello, World!
```
**Explanation:** Function pointer `func_ptr` calls the `greet()` function.

### 9. **Static vs Local Variable**
```c
#include <stdio.h>

int main() {
    int a = 5;
    static int b = 10;
    printf("%d %d\n", a, b);
    a++;
    b++;
    printf("%d %d\n", a, b);
    return 0;
}
```
**Output:**
```
5 10
6 11
```
**Explanation:** The static variable retains its value across function calls, while the local variable `a` doesn't.

### 10. **Null Pointer Dereferencing**
```c
#include <stdio.h>

int main() {
    int *ptr = NULL;
    printf("%d\n", *ptr);
    return 0;
}
```
**Output:**
```
Segmentation Fault (core dumped)
```
**Explanation:** Dereferencing a `NULL` pointer causes a runtime error.

### 11. **Multiple Return Statements**
```c
#include <stdio.h>

int fun(int a) {
    if (a > 0)
        return 1;
    return 0;
}

int main() {
    int result = fun(-5);
    printf("%d\n", result);
    return 0;
}
```
**Output:**
```
0
```
**Explanation:** The function returns 0 because the condition `a > 0` is false.

### 12. **Preprocessor Directives Confusion**
```c
#include <stdio.h>
#define MAX 100
int main() {
    printf("%d\n", MAX);
    return 0;
}
```
**Output:**
```
100
```
**Explanation:** The preprocessor directive `#define` replaces `MAX` with `100`.

### 13. **Array Index Out of Bound**
```c
#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    printf("%d\n", arr[6]);
    return 0;
}
```
**Output:**
```
Garbage value (depends on the system)
```
**Explanation:** Accessing out-of-bounds memory can result in a garbage value.

### 14. **Division by Zero**
```c
#include <stdio.h>

int main() {
    int a = 10, b = 0;
    printf("%d\n", a / b);
    return 0;
}
```
**Output:**
```
Floating Point Exception
```
**Explanation:** Division by zero causes a runtime error.

### 15. **Recursion and Stack Overflow**
```c
#include <stdio.h>

void fun() {
    fun();
}

int main() {
    fun();
    return 0;
}
```
**Output:**
```
Segmentation Fault (stack overflow)
```
**Explanation:** Infinite recursion leads to stack overflow.

### 16. **Enum Size**
```c
#include <stdio.h>

enum Color { Red, Green, Blue };

int main() {
    printf("%lu\n", sizeof(enum Color));
    return 0;
}
```
**Output:**
```
4
```
**Explanation:** The size of the enum is typically the size of an `int` (4 bytes).

### 17. **Goto Statement**
```c
#include <stdio.h>

int main() {
    int a = 10;
    if (a == 10) {
        goto skip;
    }
    printf("This won't be printed\n");

    skip:
    printf("This will be printed\n");
    return 0;
}
```
**Output:**
```
This will be printed
```
**Explanation:** The `goto` statement skips the previous block of code.

### 18. **Using Bitwise Operators**
```c
#include <stdio.h>

int main() {
    int a = 5, b = 3;
    printf("%d\n", a & b);
    return 0;
}
```
**Output:**
```
1
```
**Explanation:** The bitwise AND of `5` (0101) and `3` (0011) results in `1` (0001).

### 19. **Function Overloading in C++**
```cpp
#include <iostream>

void display(int a) {
    std::cout << "Integer: " << a << std::endl;
}

void display(double a) {
    std::cout << "Double: " << a << std::endl;
}

int main() {
    display(10);
    display(10.5);
    return 0;
}
```
**Output:**
```
Integer: 10
Double: 10.5
```
**Explanation:** Function overloading allows functions with the same name but different parameters.

### 20. **Memory Leak**
```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int *ptr = (int *)malloc(sizeof(int));
    *ptr = 10;
    free(ptr);
    return 0;
}

```
**Output:**
```
No visible output, but there is a potential memory leak here if free() is not called.
```
**Explanation:** If memory is dynamically allocated but not freed, it can lead to a memory leak.

### 21. **Virtual Functions in C++**
```cpp
#include <iostream>

class Base {
public:
    virtual void show() {
        std::cout << "Base class\n";
    }
};

class Derived : public Base {
public:
    void show() override {
        std::cout << "Derived class\n";
    }
};

int main() {
    Base *ptr;
    Derived obj;
    ptr = &obj;
    ptr->show();
    return 0;
}
```
**Output:**
```
Derived class
```
**Explanation:** Virtual functions allow dynamic dispatch at runtime.

### 22. **Const Pointer**
```cpp
#include <iostream>

int main() 
{
    int a = 10;
    int *const ptr = &a;
    *ptr = 20;  // OK
    // ptr = &b; // Error: cannot change the address ptr points to
    std::cout << *ptr << std::endl;
    return 0;
}
```
**Output:**
```
20
```
**Explanation:** A constant pointer cannot change the address it holds but can modify the value it points to.

### 23. **Static Class Members in C++**
```cpp
#include <iostream>

class Test {
public:
    static int count;
    Test() {
        count++;
    }
    static void show() {
        std::cout << count << std::endl;
    }
};

int Test::count = 0;

int main() {
    Test t1, t2;
    Test::show();
    return 0;
}
```
**Output:**
```
2
```
**Explanation:** Static members belong to the class and are shared by all objects.

### 24. **Array of Pointers**
```cpp
#include <iostream>

int main() {
    const char *arr[] = {"Apple", "Banana", "Orange"};
    for (int i = 0; i < 3; i++) {
        std::cout << arr[i] << std::endl;
    }
    return 0;
}
```
**Output:**
```
Apple
Banana
Orange
```
**Explanation:** An array of string literals is printed.

### 25. **Function Pointer with Arguments**
```c
#include <stdio.h>

void display(int num) {
    printf("Number: %d\n", num);
}

int main() {
    void (*func_ptr)(int) = display;
    func_ptr(10);
    return 0;
}
```
**Output:**
```
Number: 10
```
**Explanation:** The function pointer `func_ptr` is used to call the `display()` function.

### 26. **Template Specialization in C++**
```cpp
#include <iostream>

template <typename T>
void print(T value) {
    std::cout << "General: " << value << std::endl;
}

template <>
void print<int>(int value) {
    std::cout << "Integer: " << value << std::endl;
}

int main() {
    print(5);       // Integer: 5
    print("Hello"); // General: Hello
    return 0;
}
```
**Output:**
```
Integer: 5
General: Hello
```
**Explanation:** Template specialization provides a custom implementation for specific types.

### 27. **Reference Variables in C++**
```cpp
#include <iostream>

int main() {
    int a = 10;
    int &ref = a;
    ref = 20;
    std::cout << a << std::endl;
    return 0;
}
```
**Output:**
```
20
```
**Explanation:** A reference variable is an alias to an existing variable, and modifying it affects the original.

### 28. **Bitwise XOR and Swap**
```cpp
#include <stdio.h>

int main() {
    int a = 5, b = 7;
    a = a ^ b;
    b = a ^ b;
    a = a ^ b;
    printf("%d %d\n", a, b);
    return 0;
}
```
**Output:**
```
7 5
```
**Explanation:** Bitwise XOR can be used to swap two numbers without a temporary variable.

### 29. **Type Casting**
```cpp
#include <iostream>

int main() {
    double pi = 3.14159;
    int int_pi = (int)pi;
    std::cout << int_pi << std::endl;
    return 0;
}
```
**Output:**
```
3
```
**Explanation:** Type casting truncates the decimal part of the floating-point number.

### 30. **Lambda Function in C++**
```cpp
#include <iostream>

int main() {
    auto add = [](int a, int b) { return a + b; };
    std::cout << add(5, 10) << std::endl;
    return 0;
}
```
**Output:**
```
15
```
**Explanation:** A lambda function is defined and used to add two numbers.

These questions are designed to challenge your understanding of C, C++, and general programming concepts. Make sure to practice them thoroughly before your interview!