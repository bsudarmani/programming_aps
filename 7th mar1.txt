Here are **30 hard-level C and C++ questions** covering **pointers, strings, loops, complex nested loops, control flows, structures, unions, and function pointers**.

---

### **Pointers & Memory Management**
#### **1. What is the output of the following C program?**
```c
#include <stdio.h>
int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int *p = arr + 1;
    printf("%d\n", *(p + 2) - arr[1]);
    return 0;
}
```

#### **2. What will be printed?**
```c
#include <stdio.h>
int main() {
    int x = 10, *p;
    void *q;
    p = &x;
    q = p;
    printf("%d\n", *(int*)q);
    return 0;
}
```

#### **3. Predict the output.**
```c
#include <stdio.h>
void swap(int **p, int **q) {
    int *temp = *p;
    *p = *q;
    *q = temp;
}
int main() {
    int x = 10, y = 20;
    int *p = &x, *q = &y;
    swap(&p, &q);
    printf("%d %d", *p, *q);
    return 0;
}
```

#### **4. What happens in this code?**
```c
#include <stdio.h>
int main() {
    int arr[5] = {1, 2, 3, 4, 5};
    int *ptr = arr;
    ptr += 3;
    printf("%d\n", ptr[-2]);
    return 0;
}
```

#### **5. Identify the error in the following code.**
```c
#include <stdio.h>
int main() {
    int *p;
    printf("%d", *p);
    return 0;
}
```

---

### **Strings & Character Manipulation**
#### **6. What is the output?**
```c
#include <stdio.h>
int main() {
    char str[] = "ZohoCorp";
    printf("%s %s %s\n", &str[4], str+4, 4+str);
    return 0;
}
```

#### **7. Predict the output.**
```c
#include <stdio.h>
int main() {
    char *str = "Hello World";
    printf("%c %c %c\n", *(str+6), str[6], 6[str]);
    return 0;
}
```

#### **8. Output of this program?**
```c
#include <stdio.h>
int main() {
    char str[] = "programming";
    printf("%c\n", *(str + *(str + 8) - *(str + 7)));
    return 0;
}
```

#### **9. What will happen?**
```c
#include <stdio.h>
void fun(char *s) {
    while (*s) {
        *s++ = *s + 1;
    }
}
int main() {
    char str[] = "test";
    fun(str);
    printf("%s\n", str);
    return 0;
}
```

#### **10. What is the output?**
```c
#include <stdio.h>
int main() {
    char *s = "Zoho";
    s[0] = 'T';
    printf("%s", s);
    return 0;
}
```

---

### **Loops & Complex Nested Loops**
#### **11. What will be printed?**
```c
#include <stdio.h>
int main() {
    int i, j;
    for(i = 1; i <= 5; i++) {
        for(j = i; j <= 5; j++)
            printf("* ");
        printf("\n");
    }
    return 0;
}
```

#### **12. Predict the output.**
```c
#include <stdio.h>
int main() {
    int i = 1;
    while (i++ <= 5)
        printf("%d ", i);
    return 0;
}
```

#### **13. What is the output?**
```c
#include <stdio.h>
int main() {
    int i, j;
    for(i = 1; i <= 5; i++) {
        for(j = 1; j <= i; j++) {
            if(j == 3) break;
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
```

#### **14. What happens in this program?**
```c
#include <stdio.h>
int main() {
    int i = 5;
    while (i-- > 0)
        printf("%d ", i);
    return 0;
}
```

#### **15. Find the output.**
```c
#include <stdio.h>
int main() {
    int i, j;
    for (i = 5; i > 0; i--) {
        for (j = 5; j > i; j--)
            printf("%d ", j);
        printf("\n");
    }
    return 0;
}
```

---

### **Control Flows & Function Pointers**
#### **16. Identify the output.**
```c
#include <stdio.h>
void test(int (*p)(int, int)) {
    printf("%d\n", p(4, 3));
}
int add(int a, int b) {
    return a + b;
}
int main() {
    test(add);
    return 0;
}
```

#### **17. What happens here?**
```c
#include <stdio.h>
int main() {
    int x = 10, y = 20;
    if (x = y)
        printf("Equal");
    else
        printf("Not Equal");
    return 0;
}
```

#### **18. Predict the output.**
```c
#include <stdio.h>
#define SIZE(arr) sizeof(arr)/sizeof(arr[0])
int main() {
    int arr[] = {10, 20, 30, 40};
    printf("%d", SIZE(arr));
    return 0;
}
```

#### **19. Identify the issue.**
```c
#include <stdio.h>
struct st {
    int x;
    struct st next;
};
int main() {
    struct st temp;
    temp.x = 10;
    temp.next = temp;
    printf("%d", temp.next.x);
    return 0;
}
```

#### **20. Predict the output.**
```c
#include <stdio.h>
union test {
    int x;
    char arr[4];
};
int main() {
    printf("%d", sizeof(union test));
    return 0;
}
```

---

### **Bonus: Advanced Logical Challenges**
#### **21.**
```c
#include <stdio.h>
int main() {
    int a = 5;
    int b = (a++ * 2) + (--a);
    printf("%d", b);
    return 0;
}
```

#### **22.**
```c
#include <stdio.h>
int main() {
    int a = 5;
    int b = (a >> 1) + (a << 1);
    printf("%d", b);
    return 0;
}
```

#### **23.**
```c
#include <stdio.h>
int main() {
    int x = 5, y = 10;
    printf("%d", (x & y) + (x | y));
    return 0;
}
```

#### **24.**
```c
#include <stdio.h>
void print(int n) {
    if (n > 0) {
        print(n - 1);
        printf("%d ", n);
    }
}
int main() {
    print(5);
    return 0;
}
```

#### **25.**
```c
#include <stdio.h>
int main() {
    int a = 0;
    printf("%d", a++ + ++a);
    return 0;
}
```

---

### **Final 5 Logical Challenges**
26. Explain `const int *p` vs `int *const p`.
27. What is the difference between `void *p` and `char *p`?
28. How do function pointers work in a struct?
29. Explain **Dangling Pointers** with an example.
30. What is **undefined behavior** in C? Provide an example.

---

These questions will **test and enhance** your C and C++ expertise. Let me know if you need explanations for any! 🚀