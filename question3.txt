Sure! Here are 30 medium-level C/C++ programming problems with explanations and expected outputs:

---

**1.** What will be the output of this program?

```c
#include <stdio.h>
int main()
{
    int x = 10, y = 5;
    printf("%d %d", x / y, x % y);
    return 0;
}
```

**Output:**
```
2 0
```
**Explanation:** Integer division gives the quotient (`10 / 5 = 2`), and modulus gives the remainder (`10 % 5 = 0`).

---

**2.** What is the output of the following program?

```c
#include <stdio.h>
int main()
{
    int x = 2;
    int y = 3;
    printf("%d", (x > y) ? x : y);
    return 0;
}
```

**Output:**
```
3
```
**Explanation:** Since `x` is not greater than `y`, the ternary operator returns `y`, which is `3`.

---

**3.** Predict the output of this code:

```c
#include <stdio.h>
int main()
{
    char a[] = "Hello";
    char b[] = "World";
    printf("%s", a + 1);
    return 0;
}
```

**Output:**
```
ello
```
**Explanation:** `a + 1` points to the second character of the string `"Hello"`, so it prints `"ello"`.

---

**4.** What will be the output of this program?

```c
#include <stdio.h>
int main()
{
    int arr[] = {1, 2, 3, 4, 5};
    printf("%d", arr[2]);
    return 0;
}
```

**Output:**
```
3
```
**Explanation:** `arr[2]` accesses the third element of the array, which is `3`.

---

**5.** Predict the output:

```c
#include <stdio.h>
int main()
{
    int a = 5, b = 10;
    printf("%d", a + b++);
    return 0;
}
```

**Output:**
```
15
```
**Explanation:** The expression `a + b++` first adds `a` (5) to `b` (10), and then increments `b`.

---

**6.** What will be the output of this program?

```c
#include <stdio.h>
int main()
{
    int i;
    for (i = 1; i <= 3; i++) {
        if (i == 2) {
            continue;
        }
        printf("%d ", i);
    }
    return 0;
}
```

**Output:**
```
1 3
```
**Explanation:** The `continue` statement skips the iteration when `i == 2`.

---

**7.** What is the output of the following code?

```c
#include <stdio.h>
int main()
{
    int x = 10, y = 20;
    printf("%d", (x > y) ? x : (y > x) ? y : 0);
    return 0;
}
```

**Output:**
```
20
```
**Explanation:** The ternary operator evaluates to `y` because `y > x` is true.

---

**8.** What will be the output of this program?

```c
#include <stdio.h>
int main()
{
    int a = 7, b = 3;
    printf("%d", a / b);
    return 0;
}
```

**Output:**
```
2
```
**Explanation:** Integer division truncates the result, so `7 / 3` equals `2`.

---

**9.** Predict the output:

```c
#include <stdio.h>
int main()
{
    int x = 5;
    x = x * 2 + 3;
    printf("%d", x);
    return 0;
}
```

**Output:**
```
13
```
**Explanation:** The expression `x * 2 + 3` evaluates to `5 * 2 + 3 = 10 + 3 = 13`.

---

**10.** What will the following program output?

```c
#include <stdio.h>
int main()
{
    int x = 10;
    x = x++ + ++x;
    printf("%d", x);
    return 0;
}
```

**Output:**
```
22
```
**Explanation:** The value of `x++` is `10`, and `++x` increments `x` to `12`, so the expression evaluates to `10 + 12 = 22`.

---

**11.** What will the following code print?

```c
#include <stdio.h>
int main()
{
    int a = 4, b = 3;
    printf("%d", a & b);
    return 0;
}
```

**Output:**
```
0
```
**Explanation:** The bitwise AND of `4` (binary `100`) and `3` (binary `011`) results in `0`.

---

**12.** Predict the output:

```c
#include <stdio.h>
int main()
{
    int a = 10, b = 3;
    printf("%d", a | b);
    return 0;
}
```

**Output:**
```
11
```
**Explanation:** The bitwise OR of `10` (binary `1010`) and `3` (binary `0011`) results in `11`.

---

**13.** What is the output of the following program?

```c
#include <stdio.h>
int main()
{
    int x = 8;
    printf("%d", x >> 2);
    return 0;
}
```

**Output:**
```
2
```
**Explanation:** The right shift operator `>>` shifts the bits of `8` (binary `1000`) two positions to the right, resulting in `2` (binary `10`).

---

**14.** What will the following code output?

```c
#include <stdio.h>
int main()
{
    int a = 10, b = 20;
    int *ptr = &a;
    *ptr = *ptr + b;
    printf("%d", a);
    return 0;
}
```

**Output:**
```
30
```
**Explanation:** `*ptr` is the value of `a`, and it is updated to `a + b`, so `a` becomes `30`.

---

**15.** What is the output of this program?

```c
#include <stdio.h>
int main()
{
    int arr[] = {1, 2, 3, 4, 5};
    printf("%d", *(arr + 4));
    return 0;
}
```

**Output:**
```
5
```
**Explanation:** `*(arr + 4)` accesses the fifth element in the array, which is `5`.

---

**16.** What will the following program output?

```c
#include <stdio.h>
int main()
{
    int a = 0;
    while (a < 5) {
        a++;
        if (a == 3)
            continue;
        printf("%d ", a);
    }
    return 0;
}
```

**Output:**
```
1 2 4 5
```
**Explanation:** The `continue` statement skips the iteration when `a == 3`.

---

**17.** Predict the output of this program:

```c
#include <stdio.h>
int main()
{
    int a = 1, b = 2;
    printf("%d %d", a++, ++b);
    return 0;
}
```

**Output:**
```
1 3
```
**Explanation:** `a++` prints `1` before incrementing `a`, and `++b` increments `b` before printing `3`.

---

**18.** What will be the output of this program?

```c
#include <stdio.h>
int main()
{
    char str[] = "Programming";
    printf("%s", str + 3);
    return 0;
}
```

**Output:**
```
gramming
```
**Explanation:** `str + 3` points to the substring starting from the fourth character of `"Programming"`, which is `"gramming"`.

---

**19.** What is the output of this program?

```c
#include <stdio.h>
int main()
{
    int arr[] = {1, 2, 3, 4, 5};
    printf("%d", arr[4]);
    return 0;
}
```

**Output:**
```
5
```
**Explanation:** `arr[4]` accesses the fifth element of the array, which is `5`.

---

**20.** What will be the output of this code?

```c
#include <stdio.h>
int main()
{
    int x = 7;
    int y = 3;
    printf("%d", (x - 1) * (y + 2));
    return 0;
}
```

**Output:**
```
18
```
**Explanation:** The expression `(x - 1) * (y + 2)` evaluates to `(7 - 1) * (3 + 2) = 6 * 5 = 30`.

---

**21.** What is the output of the following code?

```c
#include <stdio.h>
int main()
{
    int x = 5;
    int *ptr = &x;
    printf("%d", *ptr);
    return 0;
}
```

**Output:**
```
5
```
**Explanation:** `*ptr` dereferences the pointer, accessing the value of `x`, which is `5`.

---

**22.** What is the output of this code?

```c
#include <stdio.h>
int main()
{
    int x = 0;
    while (x < 5) {
        x++;
        if (x == 3)
            break;
    }
    printf("%d", x);
    return 0;
}
```

**Output:**
```
3
```
**Explanation:** The `break` statement exits the loop when `x == 3`.

---

**23.** What is the output of this code?

```c
#include <stdio.h>
int main()
{
    int x = 1, y = 2;
    int z = x * y + 2;
    printf("%d", z);
    return 0;
}
```

**Output:**
```
4
```
**Explanation:** The expression `x * y + 2` evaluates to `1 * 2 + 2 = 2 + 2 = 4`.

---

**24.** What is the output of this program?

```c
#include <stdio.h>
int main()
{
    int x = 10, y = 5;
    x /= y;
    printf("%d", x);
    return 0;
}
```

**Output:**
```
2
```
**Explanation:** The `/=` operator divides `x` by `y`, so `10 / 5 = 2`.

---

**25.** What is the output of this program?

```c
#include <stdio.h>
int main()
{
    int x = 5, y = 10;
    x ^= y;
    printf("%d", x);
    return 0;
}
```

**Output:**
```
15
```
**Explanation:** The bitwise XOR of `x` and `y` results in `15`.

---

**26.** Predict the output:

```c
#include <stdio.h>
int main()
{
    int a = 10, b = 3;
    printf("%d", a / b + a % b);
    return 0;
}
```

**Output:**
```
7
```
**Explanation:** The expression `a / b + a % b` evaluates to `10 / 3 + 10 % 3 = 3 + 1 = 7`.

---

**27.** What is the output of the following program?

```c
#include <stdio.h>
int main()
{
    int x = 5;
    printf("%d", ++x * 2);
    return 0;
}
```

**Output:**
```
12
```
**Explanation:** The pre-increment operator `++x` increments `x` before multiplying, so `6 * 2 = 12`.

---

**28.** What will be the output of this program?

```c
#include <stdio.h>
int main()
{
    int x = 5;
    int y = 7;
    printf("%d", x & y);
    return 0;
}
```

**Output:**
```
5
```
**Explanation:** The bitwise AND of `5` (binary `0101`) and `7` (binary `0111`) results in `5` (binary `0101`).

---

**29.** What will the following code print?

```c
#include <stdio.h>
int main()
{
    int x = 5, y = 10;
    printf("%d", x + y - 2);
    return 0;
}
```

**Output:**
```
13
```
**Explanation:** The expression `x + y - 2` evaluates to `5 + 10 - 2 = 13`.

---

**30.** What will be the output of the following program?

```c
#include <stdio.h>
int main()
{
    int arr[5] = {1, 2, 3, 4, 5};
    printf("%d", *(arr + 2));
    return 0;
}
```

**Output:**
```
3
```
**Explanation:** `*(arr + 2)` accesses the third element of the array, which is `3`.

---

These problems cover a wide range of topics including arithmetic operations, loops, conditionals, bitwise operations, and arrays. Let me know if you need further explanations!